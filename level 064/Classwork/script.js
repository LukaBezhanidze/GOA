function sigma() {
    let num = 0; // ეს ცვლადი გამოცხადებულია ფუნქციის შიგნით var-ით local scope
    console.log(num); // აქ მუშაობს გამოაქვს 0
}

console.log(num);
// ფუნქციის გარეთ num ცვლადს ვერ ვხედავთ, რადგან ის ლოკალურია და არის ცალკე კოდის ბლოკში